<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body class="bg-gray-900">
    <div class="w-screen h-screen  p-10 text-white">
      <h1 class="text-3xl tracking-tighter mb-3" >ALL USERS</h1>
      <div class="users flex gap-7 flex-wrap ml-[70px] mt-10">
        <% if(users.length >0){ %>
          <% users.forEach(function(user){ %>
            <div class="user max-w-72 p-3 bg-gray-700 rounded-lg">
              <div name="image" class="w-full h-52 bg-gray-500 rounded overflow-hidden">
                <img src=<%= user.image%> alt="" class="w-full h-full object-cover">
              </div>
              <h3 name="name" class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
              <h5 name="email" class="text-zinc-500"><%= user.email %></h5>
               <div class="flex gap-3 text-zinc-500 mt-3">
                 <a href="/edit/<%= user.id%>" class="text-green-600">Edit User</a>
                 <a href="/delete/<%= user.id%>"class="text-red-800">Delete User</a>
               </div>
             </div>
             
             <%  })  %>
       <% } else{ %>
          <h1 text-3xl tracking-tighter mb-3>There is No Post created..</h1>
        <% } %>
       
      </div>
    
    </div>
</body>
</html>